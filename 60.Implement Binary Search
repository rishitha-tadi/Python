n=int(input())
lst=list(map(int,input().split()))
target=int(input())
low=0
high=n-1
while low<=high:
    mid=(low+high)//2
    if lst[mid]==target:
        print("Found")
        break
    elif lst[mid]<target:
        low=mid+1
    else:
        high=mid-1
else:
    print("Not Found")
